
import React from 'react';
import { Play, Calendar } from 'lucide-react';
import EnhancedButton from './EnhancedButton';
import AnimatedBackgroundCarousel from './AnimatedBackgroundCarousel';

interface HomeProps {
  onPlayClick: () => void;
  onDailyChallengeClick: () => void;
}

const Home: React.FC<HomeProps> = ({
  onPlayClick,
  onDailyChallengeClick
}) => {
  return (
    <div className="relative flex flex-col items-center justify-center h-screen w-full">
      {/* Animated Background Carousel */}
      <AnimatedBackgroundCarousel />
      
      {/* Main Content */}
      <div className="relative z-10 logo-container mb-16">
        <div className="flex flex-col items-center mb-4">
          <img 
            src="/lovable-uploads/6112694b-f789-4951-b540-4836a5594347.png" 
            alt="SmrutiSnap Icon" 
            className="w-48 h-48 mb-6 drop-shadow-2xl"
          />
          <div className="text-[#ea384c] text-[10rem] font-bold tracking-tighter leading-none mb-8 drop-shadow-2xl text-shadow-lg">
            SMRUTISNAP
          </div>
          
          {/* Buttons positioned under text */}
          <div className="flex justify-between w-full max-w-4xl gap-8">
            <EnhancedButton 
              onClick={onPlayClick} 
              animationType="scale"
              className="bg-[#ea384c] hover:bg-red-600 transition-colors flex items-center justify-center gap-4 rounded-full px-16 py-6 text-white text-2xl font-medium min-w-[280px] drop-shadow-xl shadow-lg hover:shadow-xl"
            >
              <Play size={32} />
              Play
            </EnhancedButton>
            
            <EnhancedButton 
              onClick={onDailyChallengeClick} 
              animationType="scale"
              className="bg-orange-500 hover:bg-orange-600 transition-colors flex items-center justify-center gap-4 rounded-full px-16 py-6 text-white text-2xl font-medium min-w-[280px] drop-shadow-xl shadow-lg hover:shadow-xl"
            >
              <Calendar size={32} />
              Daily Challenge
            </EnhancedButton>
          </div>
        </div>
      </div>
      
      <div className="relative z-10 mt-8 text-center text-gray-600">
        
        
      </div>
    </div>
  );
};

export default Home;
