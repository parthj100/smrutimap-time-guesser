import { createClient } from "@supabase/supabase-js"; const supabase = createClient("https://rhxbadjyjjjrjpvfhpap.supabase.co", "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJoeGJhZGp5ampqcmpwdmZocGFwIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDc5NDUyMTUsImV4cCI6MjA2MzUyMTIxNX0.Z7sfOz6dgSgjKyJspwoYPz4gbJwYRe8zxpNYV_YeonA"); async function searchAllUrls() { const { data } = await supabase.from("game_images").select("image_url"); console.log("🔍 Searching all URLs for ImgBB..."); const imgbbUrls = data.filter(img => img.image_url.toLowerCase().includes("ibb")); const bapsUrls = data.filter(img => img.image_url.includes("baps.org")); const driveUrls = data.filter(img => img.image_url.includes("drive.google")); const otherUrls = data.filter(img => !img.image_url.includes("baps.org") && !img.image_url.includes("drive.google") && !img.image_url.toLowerCase().includes("ibb")); console.log(`📊 ImgBB URLs: ${imgbbUrls.length}`); console.log(`🏛️ BAPS URLs: ${bapsUrls.length}`); console.log(`❌ Google Drive URLs: ${driveUrls.length}`); console.log(`❓ Other URLs: ${otherUrls.length}`); if (imgbbUrls.length > 0) { console.log("\n✅ Found ImgBB URLs:"); imgbbUrls.slice(0,3).forEach((img, i) => console.log(`${i+1}. ${img.image_url}`)); } } searchAllUrls();
